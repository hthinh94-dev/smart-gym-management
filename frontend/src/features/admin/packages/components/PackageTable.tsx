import type { AdminMembershipPackage } from "../types/adminPackage.types";
const money = new Intl.NumberFormat("vi-VN", { style: "currency", currency: "VND", maximumFractionDigits: 0 });
export function PackageTable({ items, isLoading, pendingId, onEdit, onDeactivate }: { items: AdminMembershipPackage[]; isLoading: boolean; pendingId?: number; onEdit: (item: AdminMembershipPackage) => void; onDeactivate: (item: AdminMembershipPackage) => void }) {
    if (isLoading) return <div className="admin-package-loading" role="status">Loading packages</div>;
    if (items.length === 0) return <div className="admin-package-empty"><h2>No packages found</h2><p>Create the first membership package for the public catalog</p></div>;
    return <div className="admin-table-shell admin-package-table"><table><thead><tr><th>Package</th><th>Duration</th><th>Price</th><th>Status</th><th>Actions</th></tr></thead><tbody>{items.map((item) => <tr key={item.id}><td><strong>{item.name}</strong><span>{item.description || "No description"}</span></td><td>{item.durationDays} days</td><td>{money.format(item.price)}</td><td><span className={`package-status ${item.isActive ? "active" : "inactive"}`}>{item.isActive ? "ACTIVE" : "INACTIVE"}</span></td><td><div className="package-row-actions"><button type="button" onClick={() => onEdit(item)} disabled={pendingId === item.id}>Edit</button><button type="button" onClick={() => onDeactivate(item)} disabled={!item.isActive || pendingId === item.id}>{pendingId === item.id ? "Working" : "Deactivate"}</button></div></td></tr>)}</tbody></table></div>;
}
